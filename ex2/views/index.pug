extends layout

block content
  table 
    thead 
      tr 
        th ID
        th Designação
        th Data
        th Nº Produtos
    for lista in listas
      tbody
        tr
          td
            a(href='/listas/' + lista._id)= lista._id
          td= lista.designacao
          td= lista.data
          td= lista.produtos.length